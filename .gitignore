__pycache__/
*.pyc

# Holds a live token. Only .env.example belongs in git.
.env
*.env
!.env.example

# Local design notes — in-repo for us, deliberately not published yet.
DESIGN.md

# Rust build output.
target/

# Scoped worktrees live in-repo so the branch and its checkout stay together.
.worktrees/
